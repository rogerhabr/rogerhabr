import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Website Cloner Agent",
  description:
    "AI-powered website cloning dashboard — enter any URL and get production-ready clone code powered by Claude.",
  keywords: ["website cloner", "AI", "Claude", "code generation", "web development"],
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  );
}

# Next.js
.next/
out/
node_modules/

# Claude Code (settings and hooks are tracked; only local/personal files are ignored)
.claude/settings.local.json

# TypeScript build cache
tsconfig.tsbuildinfo

# Misc
.DS_Store
*.pem
.env*.local
npm-debug.log*

# Transient PR body written by the throughput agent (committed only via create-pull-request add-paths)
throughput-update-report.md
